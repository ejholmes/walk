#!/bin/bash

NAME=${2%.docker}

case $1 in
  deps)
    echo Dockerfile.$NAME
    cat Dockerfile.$NAME | awk '/# depends: / { print $3 ".docker" }'
    ;;
  *)
    docker build -t walk/$NAME -f Dockerfile.$NAME .
esac
